# 문서 생성 전용 프로파일
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: false

# OpenAPI 문서를 파일로 저장
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: false
  output-dir: build/openapi
